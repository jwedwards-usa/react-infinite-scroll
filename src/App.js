import './App.css';
import { InfiniteScroll } from './infiniteScroll';

function App() {
    return ( <
        div className = "App" >
        <
        header className = "App-header" > { InfiniteScroll() } <
        /header> < /
        div >
    );
}

export default App;